<%= form_for @task, url: tasks_path do|f| %>
	<p> 
	<%= f.label :title %>
	<%= f.text_field :title %> 
	</p>
	<p>
	<%= f.label :description %>
	<%= f.text_field :description %> 
	</p>
	<p>
	<%= f.label :priority %>
	<%= f.text_field :priority %> 
	</p>
	<%= f.submit %>
<%end%>